\documentclass[aspectratio=169]{beamer}
\usetheme[numbering=fraction, progressbar=foot]{metropolis}

\usepackage{fontspec}
\usepackage{fontawesome5}

\title{ {{- title -}} }
\subtitle{ {{- subtitle -}} }
\date{\today}

\begin{document}

\begin{frame}
\maketitle
\end{frame}

\begin{frame}
    \frametitle{ {{- translations.symbols -}} }

    \faUser~ {{ translations.who }}

    \faMagic~ {{ translations.wish }}

    {% if show_types %}
        \faList~ {{ translations.types }}
    {% endif %}

    \faBullseye~ {{ translations.goal }}

    \faClock~ {{ translations.duration }}

    \faScroll~{{ translations.reso }}

\end{frame}


{%for category, ak_list in categories_with_aks %}

    \section{ {{- category.name -}} }

    {% for ak, next_aks in ak_list %}

        \begin{frame}
            \frametitle{ {% if ak.wish %}\faMagic~{% endif %} {{- ak.name | latex_escape -}} {% if ak.reso %} \faScroll{% endif %} }

            \vspace{1em}

            {% if not ak.wish %}
                \faUser~ {{ ak.owners_list | latex_escape }}
            {% endif %}

            {% if show_types and ak.types_list %}
                \faList~ {{ak.types_list }}
            {% endif %}

            {% if not result_presentation_mode and ak.durations_list %}
                \faClock~ {{ak.durations_list}}
            {% endif %}

            {% if ak.goal %}
                \faBullseye~ {{ ak.goal | latex_escape }}
            {% endif %}

            {{ ak.description | truncatechars(280) | latex_escape }}

            \vspace{2em}

            \begin{scriptsize}
                {% for n_ak in next_aks %}
                    {% if n_ak and not n_ak.wish %}\hfill \faAngleDoubleRight~ {{- n_ak.name | latex_escape_utf8 -}}{% endif %}
                {% endfor %}
            \end{scriptsize}

        \end{frame}

    {% endfor %}

{% endfor %}

\end{document}
